describe('{{pascalCase pageName}} page', () => {
  beforeEach(() => {
    cy.visit('http://localhost:3000/{{kebabCase routeNameForThePage}}');
  });

  it('should visit to the {{pascalCase pageName}} page', () => {
    cy.contains('Pagina {{pascalCase pageName}}').should('be.visible');
  });
});
